<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".KrishakApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Krishak">
        
        <activity
            android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />
        
        <activity android:name=".AddCropActivity" />
        <activity android:name=".CropDetailActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".InvestorDashboardActivity" />
        <activity android:name=".InvestmentDetailActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".OrderHistoryActivity" />
        
    </application>

</manifest>